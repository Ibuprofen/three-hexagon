{
  "name": "three-hexagon",
  "version": "1.0.0",
  "description": "",
  "main": "dist/hexagon.js",
  "scripts": {
    "build": "babel src -d dist",
    "watch": "babel src --watch -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "peerDependencies": {
    "three": "^0.77.1"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0"
  }
}
